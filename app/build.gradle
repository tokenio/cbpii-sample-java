plugins {
    id 'com.github.johnrengelman.shadow' version '2.0.0'
    id "com.google.osdetector" version "1.4.0"
}

project.apply plugin: 'application'
project.mainClassName = project.group + '.Application'

dependencies {
    compile group: 'com.sparkjava', name: 'spark-core', version: ver.sparkjava
    compile group: 'io.token.sdk', name: 'tokenio-sdk-tpp', version: ver.tokenSdk
    compile group: 'com.google.code.gson', name: 'gson', version: ver.gson
    compile group: 'org.slf4j', name: 'slf4j-simple', version: ver.slf4j
}
